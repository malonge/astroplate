---
interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---

<section class={`cv-section ${className || ''}`}>
  <h2>{title}</h2>
  <slot />
</section>
